//Author: Manos Papadakis

// This file was generated by compileAttributes
// Generator token: 10BE3573-1514-4C36-9D1C-5A225CD40393

#include <RcppArmadillo.h>
#include <Rcpp.h>
#include <vector>

using namespace Rcpp;
using namespace std;
using namespace arma;

//[[Rcpp::export]]
std::vector<double> colrange(NumericMatrix x,bool cont = true){
  unsigned int p=x.ncol();
  mat X = mat(x.begin(), x.nrow(), p, false); 
  arma::rowvec f=max(X,0)-min(X,0);
  arma::rowvec::iterator ff=f.begin();
  std::vector<double> F(p);
  std::vector<double>::iterator FF=F.begin();
  if(cont)
  	for(;FF!=F.end();++FF,++ff)
  	  *FF=*ff;
  else
  	for(;FF!=F.end();++FF,++ff)
	  *FF=*ff+1;
  return F;
}

RcppExport SEXP Rfast_colrange(SEXP xSEXP,SEXP contSEXP){
BEGIN_RCPP
    RObject __result;
    RNGScope __rngScope;
    traits::input_parameter< NumericMatrix >::type x(xSEXP);
    traits::input_parameter< bool >::type cont(contSEXP);
    __result = wrap(colrange(x,cont));
    return __result;
END_RCPP
}
